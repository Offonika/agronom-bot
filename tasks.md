# Epic: Optimization S1 — память планов и упрощение мастера

## Цель
Сделать путь «Фото → Диагноз → План → Время» максимально человечным: бот помнит недавние диагнозы, не роняет мастер при паузах, объясняет ошибки словами пользователя и даёт управлять планами как списком задач.

## Горячие правки (high)
- **BETA-601 — MVP beta houseplants (50–100 пользователей)** ✅ (size: L, DoD: `BETA_HOUSEPLANTS_ENABLED` + `BETA_TESTER_IDS`; при первом `/start` для id из ENV ставим `users.is_beta=true` (не трогаем обычных); paywall для beta не блокирует (402 не отдаём, но usage считаем); onboarding и тексты беты в локалях, дефолт/подсказка indoor без жёстких ограничений; первый диагноз создаёт `case` и `case_id` сохраняется в `recent_diagnoses` и переиспользуется в `planFlow` (без дублей); мини‑опрос после первого диагноза с inline Q1/Q2 и опциональным Q3, результат в `diagnosis_feedback`, повторно не спрашиваем (`survey_completed_at`); follow‑up через `BETA_FOLLOWUP_DAYS=3` + один ретрай через +2 дня при отсутствии ответа, ответы в `followup_feedback`; события в `beta_events` + SQL/скрипт выгрузки и опционально `/beta_stats` (HMAC); обновлены `docs/srs.md`, `docs/data_contract.md`, `docs/prd.md`, минимальные тесты/линты указаны в ответе)
- **BUG-501 — привязать диагноз к объекту по культуре** ✅ (size: M, DoD: `recent_diagnoses` и кейс получают `object_id` из AI-ответа/созданного объекта; `planFlow` сначала использует `diagnosis.object_id`, затем подбирает по `type`/crop, только потом падает на `last_object_id`; повторный `/plan_treatment` по тому же диагнозу не предлагает старые объекты; обновлены копирайт/локали если нужно)  
- **BUG-502 — дедуп /plan_treatment и сессий выбора объекта** ✅ (size: S, DoD: двойной клик по CTA или повторный вход в течение нескольких секунд не создаёт новую `plan_session`; используются throttle/переиспользование активной сессии по `recent_diagnosis_id`; логируется один `plan_flow.start`)  
- **DX-901 — сохранение сорта/культивара** ✅ (size: M, DoD: в `DiagnoseResponse` есть `variety`/`variety_ru`, поле сохраняется в `cases.raw_ai`, `recent_diagnoses.diagnosis_payload` и `objects.meta.variety` при создании/обновлении; сорт показывается в мастере/плане; обновлены `docs/data_contract.md` и `docs/srs.md`)  
- **OBJ-801 — редактирование и удаление растений** ✅ (size: S, DoD: в /edit доступны переименование и удаление объектов; удаление блокируется при наличии активных планов; обновлены локали и docs)
- **PAY-601 — SBP mock webhook (invoice + autopay)** ✅ (size: S, DoD: `scripts/mock_bank.js` для invoice/autopay, `SBP_MODE` в env, обновлена `docs/payment_flow.md`)
- **PAY-602 — analytics_events вместо events для логов** ✅ (size: S, DoD: таблица `analytics_events`, `Event` пишет туда, `docs/data_contract.md` обновлён)
- **BOT-701 — меню подписки и автопродления** ✅ (size: XS, DoD: команды `/subscribe`, `/autopay_enable`, `/cancel_autopay` видны в меню бота)
- **PAY-603 — Tinkoff test API (Init + GetQr + GetState)** ✅ (size: M, DoD: `SBP_MODE=tinkoff_test` делает запросы в Tinkoff API, возвращает реальный SBP URL, сохраняет `provider_payment_id`, а `/v1/payments/{id}` умеет синхронизировать статус через GetState; env и `docs/payment_flow.md` обновлены)
- **PAY-604 — кнопки оплаты картой + СБП в боте** ✅ (size: S, DoD: `/v1/payments/create` возвращает `url` (карта) и опциональный `sbp_url`; бот показывает 1–2 кнопки с корректными текстами; OpenAPI/доки и локали обновлены)

## Фаза 1 — Устойчивость и память (high) — ответственный: Codex
- **DX-101 — recent_diagnoses** ✅ (size: M, DoD: /plan_treatment работает без нового фото в течение 24 ч)  
  Таблица `recent_diagnoses (id, user_id, object_id?, payload, case_id?, plan_id?, created_at, expires_at)` заведена, API `/v1/diagnoses/recent`/`/recent/{id}` доступны, бот предлагает «Использовать/Новое фото» или «Открыть план/Создать новый» в течение 24 ч без повторной загрузки.
- **DX-102 — plan_sessions** ✅ (size: L, DoD: мастер восстанавливается после рестарта)  
  Таблица `plan_sessions` заведена; API `/v1/plans/sessions` (create/get/patch/delete) доступен, bot/planFlow хранят состояние шагов (choose/pick/time) и восстанавливают его после рестартов. TTL/истечение обрабатывается с дружелюбными текстами из DX-103.
- **DX-103 — мягкий копирайтинг ошибок** ✅ (size: M, DoD: каждая ошибка выводит дружелюбный текст + кнопку действия)  
  Коды причин (LOW_CONFIDENCE, SESSION_EXPIRED, OBJECT_NOT_OWNED, BUTTON_EXPIRED, PLAN_NOT_FOUND, NO_RECENT_DIAGNOSIS) реализованы в `bot/userErrors.js`; тексты/кнопки находятся в `locales/ru.json` и используются в `plan_treatment`, выборе объекта, pick_opt и других ветках.
- **DX-104 — воронка UX** ✅ (size: S, DoD: есть SQL/дашборд со всеми шагами конверсии)  
  В таблицу `plan_funnel_events` логируются шаги `photo_received → diagnosis_shown → plan_treatment_clicked → object_selected → option_picked → slot_confirmed`, есть SQL-отчёт (см. `docs/LOGGING.md`) для мониторинга конверсии.

## Фаза 2 — Упрощение UX (medium) — ответственный: Codex
- **UX-201 — авто-объект + индикаторы** ✅ (size: M, DoD: пользователи с 1 объектом попадают в таблицу без лишних шагов)  
  PlanFlow при единственном объекте автоматически показывает план/таблицу, а все экраны мастера снабжены текстами «Шаг 1/3, 2/3, 3/3» и кнопками «⬅ Назад / ✖ Отмена» (`plan_step_*` строки и `buildStepControls`).
- **UX-202 — чипы как главный вход** ✅ (size: M, DoD: максимум 1 клик до подтверждения объекта)  
  При нескольких объектах план-флоу выводит чипы (`objectChips`) и инлайн-список, «Шаг 1/3…» (`plan_step_choose_chips`). Клик активирует растение, показывает подтверждение и сразу запускает план.
- **UX-203 — короткий путь и онбординг** ✅ (size: M, DoD: типовой сценарий — 3 шага после диагноза)  
  При первом запуске показываем онбординг/демо-план (`plan_onboarding_steps`, `plan_demo`), для уверенного диагноза + одного объекта путь “Фото → План → Время” автоматически сокращается до 3 шагов (см. `maybeSendShortPathHint`, `plan_single_auto`).

## Фаза 3 — Планы как список задач (medium) — ответственный: Codex
- **PM-301 — раздел «📋 Мои планы»** ✅ (size: M, DoD: можно отметить/перенести/отменить событие без нового фото)  
  Команда/кнопка показывает ближайшие обработки по объектам с кнопками `[✅ Выполнено] [🔁 Перенести] [✖ Отменить] [Открыть план]`. Есть фильтр по объектам, пагинация «Показать ещё», группировка по планам, блок «Просроченные» с массовыми действиями и переходом в ручной мастер времени. Используется и как fallback при ошибках.
- **PM-302 — меню и команды** ✅ (size: S, DoD: главное меню Telegram содержит «Новый диагноз / Мои планы / Мои растения»)  
  Закреплены команды `/new`, `/plans`, `/objects`; раздел «Мои планы» доступен из push-уведомлений и fallback’ов. Команды `/objects`, `/plans`, `/use` остаются дополнительными.

## Фаза 4 — Время и напоминания (medium-low) — ответственный: Codex
- **TIME-401 — карточка автоплана** ✅ (size: M, DoD: после `pick_opt` пользователь видит слот с объяснением и может утвердить/изменить)  
  Карточки «Шаг 3/3…» формируются в `plan_pick`/автоплан-воркере: показываем окно, причины (`reason[]`), кнопки `[Принять] [Выбрать другое время] [Отменить]`, при недоступности очереди создаётся fallback-slot/ручной CTA.
- **TIME-402 — ручные слоты и напоминания** ✅ (size: M, DoD: при отсутствии автоплана пользователь выбирает время за ≤3 клика)  
  Callback `plan_manual_start` запускает быстрые пресеты («Сегодня вечером», «Завтра утром», «Выбрать дату»), создаёт события/напоминания и позволяет подтверждать время без дополнительных команд.
- **TIME-403 — связь с plan_sessions** ✅ (size: S, DoD: карточки времени восстанавливаются после паузы)  
  `plan_sessions` сохраняет шаги `time_autoplan_lookup/time_autoplan_slot/time_manual_prompt/time_scheduled`; `/plan_treatment` восстанавливает карточку после паузы, автоплан и ручные слоты обновляют состояние.

## Backlog (low)
- Публичный демо-план без создания напоминаний. ✅
- Аналитика популярных объектов/болезней для подсказок. ✅
- ML-ранжирование зелёных окон (из прежнего backlog). ✅
- **OBJ-701 — сорт/метка участка при создании объекта** ✅ (size: M, DoD: после диагноза бот спрашивает сорт и метку участка/ряда, сохраняет в `objects.meta.variety`/`meta.note` и показывает в чипах/«Мои планы»; можно отредактировать через /objects; дефолт «Пропустить» не мешает пути)  
- **OBJ-702 — защита от дублей объектов** ✅ (size: M, DoD: при создании объекта той же культуры/локации/имени предлагается «Новый / Та же — объединить / Переименовать»; планы/напоминания не размножаются, есть команда `/objects merge` для склейки дублей и переноса планов; проверки в план-флоу предпочитают существующий объект, если совпадает культура/метка)  

## Фича: мультифото диагноз и подсказки (high) — ответственный: Codex
- **PHOTO-501 — подсказки по съёмке** ✅ (size: M, DoD: в бот добавлен онбординг «Как сфоткать» с 4 карточками)  
  Карточки: ① общий вид растения, ② лист лицевая, ③ лист изнанка, ④ плод/цветок/корень (по ситуации). Краткие советы про свет/фокус/без блюра. Кнопка «Как сфоткать?» доступна из `/new` и мастера загрузки, отправляется 1 раз на сессию.
- **PHOTO-502 — приём медиагруппы и минимум снимков** ✅ (size: L, DoD: бот принимает альбом и не стартует анализ без базового набора)  
  План: требуем минимум 3 фото (общий + лист лицевая + лист изнанка), плод/корень делаем опциональными. Поддержка медиагруппы (`media_group_id`) и последовательных одиночных фото в одном стейте. Есть подтверждение «Принял N фото» и счётчик попыток; лимит 8 фото, остальное вежливо игнорируем с подсказкой.
- **PHOTO-503 — чеклист и дозаказ недостающих** ✅ (size: M, DoD: после загрузки бот показывает чеклист и просит дослать недостающее)  
  Чеклист в тексте: «Общий ✓/✗, Лист лицевая ✓/✗, Лист изнанка ✓/✗, Плод ✓/✗, Корень ✓/✗». Если нет базовых кадров, просим дослать кнопкой «Добавить фото» или «Пропустить плод/корень». После полного минимума — кнопка «Готово — анализ».
- **PHOTO-504 — тексты, локали, тесты** ✅ (size: S, DoD: строки в `locales/ru.json`, есть unit/e2e на сбор альбома)  
  Обновляем копирайтинг для подсказок/ошибок (таймауты, лишние фото, пропуск опциональных кадров). Покрываем `albumCollector`/state-транзишены юнит-тестами и один e2e: последовательность «подсказки → альбом 4 фото → чеклист → анализ».

## Фича: координаты и точные окна (high) — ответственный: Codex
- **GEO-401 — объект с координатами + /location** ✅ (size: M, DoD: каждый объект может хранить lat/lon и пользователь может их поменять).  
  В `objects.meta` храним числовые `lat/lon`, валидация диапазона (check constraint + sanitize), при некорректном патче координаты не затираются. Команда `/location` и кнопки обновления локации обновляют meta с `location_source`/`location_updated_at`; лимитируем частые запросы, есть сообщения об истечении/лимите.
- **GEO-402 — сервис геокодера с кешем** ✅ (size: M, DoD: геокодер отдаёт координаты ≤1 сек и не упирается в лимиты).  
  OSM Nominatim с таймаутами и ретраями, кеш в Redis (`geo:{query}`, TTL 24 ч), rate limit per user (Redis/in-memory), логирование таймаутов/ошибок и хранение confidence/label.
- **GEO-403 — автодетект в план‑флоу** ✅ (size: M, DoD: после диагноза бот предлагает «Нашли участок возле …?» с картой).  
  Запрашиваем локацию через геокодер, сохраняем в объект, показываем карточку с кнопкой карты (OSM) и `✅ Верно` / `✏️ Указать другое место`. Троттлинг: повторные карточки не чаще 30 мин, подтверждение хранится 12 ч; события логируются с источником.
- **GEO-404 — ручной выбор и UX таймаутов** ✅ (size: L, DoD: пользователь может отправить геолокацию или адрес, отменить или повторить).  
  Инлайн-кнопки `📍 Геолокация`, `⌨️ Ввести адрес`, `↩️ Отмена`. Сессия ожидания 2 мин с повторными подсказками; TTL повторного запроса 30 мин, лимит попыток; можно отменить. После выбора сохраняем источник `manual` и таймстемп, показываем карту в автодетекте, логируем.
- **GEO-405 — тексты и подсказки** ✅ (size: S, DoD: все новые шаги имеют дружелюбные тексты).  
  Добавляем в `locales/ru.json` инструкции, ошибки геокодера, уведомления о таймаутах и CTA «пропустить». Проверяем, что `msg()` не падает.
- **GEO-406 — автоплан и мониторинг** ✅ (size: M, DoD: воркер всегда сообщает, какие координаты использует).  
  `worker/autoplan` берёт координаты из объекта, логирует источник (`auto/manual/default`), при дефолте отправляет пользователю подсказку обновить локацию. Фолбэк на `.env` оставляем. Собираем метрики по доле дефолтов и ошибкам геокодера, алертим при росте.
- **GEO-407 — тесты и e2e сценарий** ✅ (size: M, DoD: CI покрывает весь путь «диагноз → локация → автоплан»).  
  Юнит-тесты на геокодер, кеш, новые состояния `planFlow`, обработку `location`/адреса/таймаутов. E2E тест прогоняет диагноз → автодетект → подтверждение/ручной ввод → автоплан → слот, проверяем воркер на подтверждённых и дефолтных координатах.

## Приоритезация
1. Фаза 1 — база устойчивости: память диагнозов и сессий + понятные ошибки.
2. Фаза 2 — сокращаем путь пользователя (чипы, шаги, онбординг).
3. Фаза 3 — даём «список задач» и навигацию без новых фото.
4. Фаза 4 — автоматизируем время/напоминания.
5. Фича GEO — внедряем параллельно TIME-401/402, чтобы зелёные окна стали точнее.
6. Backlog — подключаем по мере освобождения ресурсов.

## Marketing MVP v2.4 (январь 2026) — ответственный: Codex
Цель: рост MAU indoor-сегмент, конверсия Free→Pro, retention D7.

- **MARK-001 — Модель кейсов (week-based)** ✅ (size: L)
- **MARK-002 — 24ч пробник (trial_ends_at)** ✅ (size: S)
- **MARK-003 — Low confidence не списывает кейс** ✅ (size: M)
- **MARK-004 — UTM parsing в /start** ✅ (size: S)
- **MARK-005 — «Это то же растение?»** ✅ (size: M)
- **MARK-006 — Кнопка «Поделиться»** ✅ (size: S)
- **MARK-007 — Тексты paywall** ✅ (size: XS)
- **MARK-008 — События start+UTM, share_clicked** ✅ (size: S)

Backlog marketing:
- [x] MARK-009 — SQL: retention когорты D1/D7/D14
- [x] MARK-010 — SQL: Low confidence % по дням

Добавлено:
- **MARK-009 — SQL: retention когорты D1/D7/D14** ✅ (size: S, DoD: `scripts/marketing_reports.sql` с cohort retention)
- **MARK-010 — SQL: Low confidence % по дням** ✅ (size: S, DoD: там же, + daily funnel, UTM, trial, shares)
