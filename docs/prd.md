# PRD · «Карманный агроном» (MVP) — итоговая версия

Версия: 1.1 · Дата: 13.11.2025 (v1.0 → v1.1: добавлен пилот Variant 2 «Кто первый начал смену»)
Форм-фактор MVP: Telegram-ассистент (чат). Дальше — мобильное приложение.

## 1) Визия и цель релиза

Смартфон садовода/дачника/любителя комнатных растений становится личным агро-ассистентом:
Фото → диагноз → законный план обработки → дневник и напоминания, с учётом погоды и региона.

Цель MVP: доказать ценность на 3 ключевых сценариях (огород, сад, комнатные), получить первые платные конверсии в Pro и валидировать ретеншн через напоминания и «окна погоды».

## 2) Аудитория и персоны

- Дачник-новичок (огород: томаты/огурцы): «Скажите, что делать и когда».
- Садовод (яблоня/груша): «Нужны курсы обработок, PHI, погода, ротация ДВ».
- Любитель комнатных (монстера, фикус и т.п.): «Быстрый диагноз и простой план, напомнить повтор».

## 3) Объём MVP (Scope)

- **Диагностика по фото (лист/плод/стебель) + короткое объяснение «почему так».**
- **Многоуровневый ответ ассистента:** блоки «📸 Диагноз», «🧪 Почему так», «🧴 План», «⏰ Что дальше» с CTA.
- План обработки: препарат, доза, способ внесения, PHI, базовая безопасность; пометка региона (если применимо).
- Дневник: объекты (грядка/дерево/комнатное), события (диагноз/обработка/фото «после»), статусы (запланировано/выполнено/пропущено).
- Напоминания: обработка и PHI (привязаны к фактической дате обработки).
- Погода: «зелёные окна» на 72 часа (дефолтные пороги: дождь ≤ 0.2 мм/ч, ветер ≤ 5 м/с, t 10–25 °C), мониторинг осадков 6–8 ч после обработки.
- PDF-заметка (лайт): карточка случая/объекта/периода (для печати/шеринга).
- Тарифы и оплата: Free (5 фото/мес), Pro-Month 349 ₽/мес, Pro-Year 3 390 ₽/год; SBP, автоплатёж opt-in (уведомление −3 дня).

**Не входит (Out of scope, vNext)**

- Покупка у партнёра (кнопка «Купить») — исключена из MVP.
- NDVI/спутник, оффлайн-CV, видео-звонки, Team/B2B, Export-MRL — после MVP.
- Нативные приложения на релизе MVP (чат-интерфейс Telegram — да).

## 4) Пользовательские сценарии (MVP)

- **U1 · Томаты/фитофтора (Free)**
  Фото → диагноз (≥ 0.6) → план → выбор зелёного окна (сегодня 19:00–22:00) → напоминание «обработка» → напоминание PHI через Z дней → PDF-заметка.
- **U2 · Яблоня/парша (Pro)**
  Фото → диагноз → курс 1–3 (ротация ДВ) → планирование всех трёх с учётом «окон» → мониторинг дождя после «Обработки 1» → при осадках > X мм в 6–8 ч — алерт «перенести/повторить?» → статусы «выполнено».
- **U3 · Монстера/пятнистость (Free/Pro)**
  Фото → диагноз → план без ветра/осадков → «повтор через N дней» → напоминание → фото «после» → PDF-заметка.

### Диалоговый ответ бота

- Один месседж с блоками: «📸 Диагноз» (культура, болезнь по-русски, уверенность), «🧪 Почему так» (2–4 коротких пункта из reasoning[]), «🧴 Что делать» (3–5 шагов из treatment_plan), «⏰ Что дальше» (зелёное окно, PHI, помощь с препаратами) и блок подсказок «Спросите меня…».
- CTA-кнопки: «Запланировать обработку», «Поставить напоминание PHI», «PDF-заметка», «Спросить про препараты».

## 5) Память плана и автоплан (MVP)

### 5.1 Память плана (ядро MVP)

- **Сущности:** object (грядка/дерево/комнатное), case (диагноз по объекту), plan, plan_stage, stage_option, event/reminder.
- **Пакет от ИИ:** каждая карточка возвращает текст «для человека» и машинный JSON (см. SRS), где описаны object_hint, diagnosis, stages[].options[].
- **Сценарий:** после диагноза пользователю показывается таблица этапов (например «до цветения», «после дождя») с до трёх вариантов на этап; выбор варианта фиксирует дозу/препарат и автоматически ставит напоминания.
- **Ограничения:** максимум 5 этапов на план, до 3 вариантов на этап, дозировки приводятся к каталожной единице, stage_option знает phi_days и метод внесения.
- **Статусы:** draft (черновик с последнего PLAN_NEW/PLAN_UPDATE), proposed (показан пользователю), accepted (подтверждён), scheduled (есть события/напоминания), superseded/rejected (устарел/отклонён). «Истина» на экране — всегда последний accepted/scheduled для object+case.

### 5.2 Мульти-объекты

- Активный объект выбирается inline под карточкой диагноза или командами `/objects`, `/use`.
- Каждый план несёт object_id и case_id. Если object_hint не совпадает с активным, бот предлагает выбрать «🍅 Грядка №2» или «🍆 Грядка №3».
- Смена объекта не затирает старые планы: черновики и напоминания лежат отдельно по объектам.
- История действий и напоминания фильтруются по текущему объекту, чтобы не было кросс-перекрёстных апдейтов.

### 5.3 Фоллоу-апы

- Таблица ключевых слов для реактивации чата (например «ещё что-то», «а если дождь»).
- Если в сообщении нет ключей — дефолт-фоллоу-ап со ссылкой на FAQ и CTA «Записать план вручную». Запрещено повторять одну и ту же стартовую фразу более двух раз подряд.
- Фоллоу-ап хранит ссылку на последнюю карточку, чтобы следующий ответ не начинался с «Фото приняли...» повторно.

### 5.4 Auto-Planning (MVP)

- Без ML: правило «зелёных окон» по погодному прогнозу на 72 часа (шаг 30 минут).
- Ограничения: дождь ≤ 0.2 мм/ч, выдержка до/после дождя, ветер ≤ 5 м/с (регионально настраивается), температура 10–25 °C, дневное время по умолчанию.
- Анти-дребезг: если модель прислала тот же план (SHA-1 канонического JSON) — не показываем повторно; мелкие правки «разрешён аналог» считаем безопасными и применяем без пересогласования.
- Автоплан выдаёт предпросмотр карточкой: слот, краткое «почему этот слот» (осадки 0 мм, ветер 3 м/с, +12 °C) и кнопки «Принять / Изменить / Сделать вручную».
- Если подходящих окон нет — статус `awaiting_window`, черновик хранится и пересчитывается при обновлении прогноза.

### 5.5 Критерии приёмки (DoD)

- После диагноза достаточно 1–2 нажатий, чтобы выбрать вариант и поставить напоминания; UI не заставляет вводить текст.
- Рестарт бота или повторный вход не теряет напоминания и выбранные варианты (persist в БД).
- Автоплан всегда идёт через карточку предпросмотра с выбором «Принять/Изменить», а также коротким объяснением выбора окна.
- При наличии запланированных событий бот никогда не меняет их молча: дифф показывается до подтверждения.
- Документация (SRS, API, алгоритм) описывает формат машинного пакета, жизненный цикл плана и правила автоплана.
- Всегда предлагаем 3–4 инлайн-подсказки («Что это простыми словами?», «Какие препараты в регионе?», «Безопасно ли есть?», «Что делать, если пятна остались?»).
- При confidence < 0.6 или need_reshoot=true добавляем карточку «⚠️ Пересъёмка / уточнить культуру» с советами и кнопкой «Переснять фото».
- Если модель не уверена в crop — вопрос «Это виноград/томат/комнатное?» и crop_hint повторно пробрасывается при следующем /diagnose.

## 5) Функциональные требования (FR)

### 5.1 Диагностика и объяснение

- **FR-D-01:** Приём JPEG ≤ 2 МБ; проверка качества (BLUR/NO_LEAF).
- **FR-D-02:** Ответ: crop, disease, **disease_name_ru**, confidence [0..1], reasoning[] (2–4 коротких пункта), treatment_plan {product, substance, dosage_value/unit или fallback dosage, method, phi_days, safety_note}, next_steps {reminder, green_window, CTA}, а также need_reshoot + reshoot_tips[]. Формат — чистый JSON, без fenced-кода.
- **FR-D-03:** При низкой уверенности (<0.6) — показать блок «Пересъёмка / уточнить культуру»: советы по фото, просьба уточнить культуру/стадию, CTA «Переснять фото», ссылка на поддержку вместо кнопки «Эксперт».
- **FR-D-04:** Тон ассистента дружелюбный («Давай», «Советую», «Обрати внимание»), финальный CTA — «Добавить обработку» или «Выбрать окно» (при уверенности ≥0.6) либо «Переснять фото» (иначе).
- **FR-D-05:** Бот может принять crop_hint от пользователя (инлайн «Виноград/Томат/Комнатное») и пробросить его в LLM, чтобы повысить точность.

### 5.2 План обработки

- **FR-P-01:** Карточка: препарат, доза (value+unit), способ, PHI (дни), меры безопасности.
- **FR-P-02:** Пометки региона/истечения записи; предупреждение при просроченном/скором истечении.
- **FR-P-03:** Кнопка «Запланировать» создаёт событие + подбирает окно погоды.
- **FR-P-04 (Pro):** Курс 1–3 с графиком и ротацией ДВ (предупреждение при повторе класса).

### 5.3 Дневник/объекты/статусы

- **FR-L-01:** Объект: имя, тип (bed/tree/indoor), локация (outdoor/indoor), geo? заметки.
- **FR-L-02:** События: Диагноз, Обработка (1/2/3), Фото «после», Сбор/PHI.
- **FR-L-03:** Статусы: запланировано/выполнено/пропущено; отметка «выполнено» из уведомления.
- **FR-L-04:** История и фильтры по объектам/периодам.

### 5.4 Погода и «окна»

- **FR-W-01:** Авто-гео (по согласию) или ручной регион/локация.
- **FR-W-02:** Почасовой прогноз 72 ч; пороги по умолчанию (дождь/ветер/темп).
- **FR-W-03:** Показывать 2–3 ближайших 🟢 окна; разрешить планирование в них.
- **FR-W-04:** Мониторить осадки 6–8 ч после обработки; при > X мм (дефолт 3–5 мм) — уведомление «перенести/повторить?». 

### 5.5 Напоминания

- **FR-R-01:** Создать напоминание «Обработка (дата/время)».
- **FR-R-02:** Создать PHI-напоминание от фактической даты обработки.
- **FR-R-03 (Pro):** Автоперенос на следующее 🟢 окно, если прогноз/факт дождя мешает обработке.
- **FR-R-04 (Pro):** Повторные напоминания; «тихий режим» ночью.

### 5.6 PDF (лайт)

- **FR-F-01:** Экспорт PDF по случаю и объекту/периоду.
- **FR-F-02:** Поля: культура, диагноз, план (препараты/дозы), даты, PHI, фото «до/после».

### 5.7 Пересъёмка и поддержка

- **FR-S-01:** При confidence < 0.6 бот показывает карточку «Пересъёмка / уточнить культуру» с советами: один лист в кадре, без пересвета, на уровне груди, сфокусировать прожилки.
- **FR-S-02:** В этом же блоке — CTA «Переснять фото», просьба указать культуру/стадию и ссылка на поддержку (Telegram @agrobot_support). Никаких «Эксперт»-кнопок.

### 5.8 Тарифы/лимиты/оплата

- **FR-T-01:** Free: лимит 5 фото/мес; при превышении — paywall.
- **FR-T-02:** Pro-Month/Year: активация после SBP-успеха; автоплатёж opt-in, уведомление −3 дня.
- **FR-T-03:** Grace 3 дня при ошибке списания.

## 6) UX-флоу (high-level)

Диагноз → 2) План (кнопка «Запланировать») → 3) Окно погоды (выбор) → 4) Событие в дневнике (статус «запланировано») → 5) Напоминание обработки → 6) Отметить «выполнено» (+ фото «после») → 7) PHI-напоминание → 8) PDF при необходимости → 9) При низкой уверенности — блок «Пересъёмка / уточнить культуру» + ссылка на поддержку.

Тексты — простые, без жаргона; единицы — ГОСТ (мл/10 л, л/га и т.п.).

## 7) Критерии приёмки (Acceptance · Given/When/Then)

- **A1 · Томаты/фитофтора (Free)**
  - **Given** Free-пользователь и фото хорошего качества
  - **When** отправляет фото
  - **Then** видит диагноз (≥ 0.6), объяснение, кнопку «План»
  - **And** в плане — препарат/доза/PHI; показываются 2–3 🟢 окна
  - **And** после выбора окна создаётся напоминание, событие в дневнике
  - **And** по сроку приходит PHI-напоминание
- **A2 · Яблоня/парша (Pro)**
  - **Given** активен Pro
  - **When** выбран курс 1–3 и даты назначены по «окнам»
  - **Then** при осадках > 4 мм в 6–8 ч после «Обработки 1» приходит уведомление
  - **And** пользователь переносит/повторяет; статусы обновляются
  - **And** по завершении курса видит 3/3 «выполнено»
- **A3 · Монстера/пятнистость**
  - **Given** объект «в помещении»
  - **When** отправлено фото
  - **Then** план без ветра/осадков, напоминание «повтор через N дней»
  - **And** формируется PDF по объекту/периоду
- **A4 · Лимит/Оплата**
  - **Given** Free и 5 фото уже израсходованы
  - **When** отправляется 6-е
  - **Then** возвращается 402 PAYWALL
  - **And** после успешного SBP — повтор запроса (или кнопка «Повторить»)

## 8) Метрики успеха (T+90 дней)

- Free → Pro-Month ≥ 8–12 %
- Завершённые планы у Pro ≥ 60 %
- Повторные фото «после» ≥ 40 %
- p95 диагностики < 8 с
- MAU ≥ 3 000
- NPS Pro ≥ 45

## 9) Данные и правила (product-level)

- Объект: {id, user_id, name, type[bed/tree/indoor], location[outdoor/indoor], geo?, notes}
- Случай (case): {id, object_id, photo_id, crop, disease, confidence, plan_id, created_at}
- План (plan): {id, case_id, substance, product, dosage_value, dosage_unit, method, phi_days, region_tag, safety_note}
- Событие (event): {id, object_id, type[diagnosis/treatment/photo_after/harvest], due_at, status, weather_window?, rain_monitor_mm?, completed_at}
- Погода: кэш почасового прогноза ≤ 6 ч; «окна» пересчитываются при планировании и перед событием.
- Порог по умолчанию: дождь ≤ 0.2 мм/ч; ветер ≤ 5 м/с; t 10–25 °C; «дождь после» — 6–8 ч, суммарно > 3–5 мм.

## 10) Нефункциональные (NFR)

- **Производительность:** p95 диагностики < 8 с; 99.5 % доставляемость напоминаний.
- **Надёжность:** доступность ≥ 99.5 %/мес; фолбэк при недоступности источника погоды (кэш/ручной выбор).
- **Стоимость:** OPEX GPT ≤ 0.50 ₽/фото (jpeg ≤ 512 КБ, кэш по хешу).
- **Приватность:** гео — по согласию; экспорт/удаление данных по запросу (SLA 30 дн.); фото в S3 с TTL 90 дн., в ML-датасет — только при opt-in.
- **Локализация:** RU; единицы — ГОСТ.

## 11) Риски и смягчение

- Низкая уверенность/качество фото → блок «Пересъёмка / уточнить культуру», подсказки + ссылка на поддержку.
- Данные погоды недоступны → кэш ≤ 6 ч, ручной выбор времени, чёткие предупреждения.
- Ошибки оплат/автосписаний → grace 3 дня, повтор, удобная отмена автоплатежа.
- Рост OPEX → сжатие JPEG, кэш диагнозов по хешу, лимиты Free.

## 12) Роли и ответственность (RACI · продукт)

| Область | R (делает) | A (утверждает) | C (консульт.) | I (в курсе) |
|---------|------------|----------------|---------------|-------------|
| One-pager / PRD | PO | PO | Агроном, Техлид | Инвесторы, Dev |
| Пороговые правила погоды | PO | PO | Агроном, Техлид | QA |
| Таксономия планов/ДВ | Агроном | PO | Техлид | Dev, QA |
| Лимиты/монетизация | Техлид | PO | FinOps | Dev, QA |
| PDF-шаблоны/тексты | PO/UX | PO | Агроном | Dev |
| Блок «Пересъёмка/поддержка» | PO | PO | Агроном, Support | Dev, QA |

## 13) План релиза (6 недель)

- **Нед.1** — утверждение PRD/макеты; финализация порогов погоды; тексты.
- **Нед.2** — SRS + схема БД + OpenAPI; каркас «diag/plan/logbook/reminders/weather».
- **Нед.3** — Диагноз/План/Дневник/Напоминания (обработка/PHI).
- **Нед.4** — Pro-подписка (лимиты, paywall, SBP), «окна» погоды, мониторинг дождя.
- **Нед.5** — PDF-заметка, блок «Пересъёмка / уточнить культуру», полировка UX-копирайта.
- **Нед.6** — QA/UAT по U1–U3, фиксы, релиз, сбор метрик.

## 14) Соответствие SRS (связка документов)

Эндпоинты /v1/ai/diagnose, /v1/photos, /v1/limits, платежные вебхуки — в SRS.

В этом PRD добавлены продуктовые сущности и поведение: дневник/объекты/события, окна погоды, PHI-напоминания, курс 1–3 (Pro), PDF-заметка, эксперт-кейс — для отражения в SRS (API/БД).

## 15) Пилот: вариант 2 «Кто первый начал смену — тот исполнитель»

- Рабочая таблица `Runs` живёт в Google Sheets: одна строка на магазин (shop_id) и календарную дату. Формат строки: `2025‑11‑13 | shop_a | executor: @olga_cashier | status: in_progress | updated_at: 11:05`.
- Столбцы: `shop_id`, «Название», «Менеджер» (Telegram handle), `status ∈ {waiting,in_progress,done}`, «Исполнитель» (username + fallback tg_id), «Последнее обновление». Доп. служебные поля (`executor_tg_id`, `started_at`, `updated_by`) прячем в конце листа.
- Бот фиксирует Telegram ID при первом `/start` и всегда пишет его в таблицу, даже если у человека нет username — менеджер видит, кого назначило правило «первый зашёл».

### Поток смены

1. Кассир нажимает «Начать смену» → бот проверяет, есть ли активная строка на текущую дату и `status=in_progress`.  
2. Если строки нет, бот создает запись (`status=in_progress`, `executor=@username/tg_id`) и возвращает чек-лист.  
3. Если строка уже занята, сотрудник получает текст: «Смена уже начата сотрудником @olga_cashier. Если вы заменяете коллегу — обратитесь к менеджеру».  
4. Напоминания по открытию смены продолжают отправляться всем сотрудникам магазина, т.к. расписания нет — ответственность за контроль на менеджере.  
5. При закрытии чек-листа бот ставит `status=done` и не создаёт новых строк до следующей даты.

### Управление менеджером

- В карточке магазина менеджер видит кнопку «Передать смену»: бот предлагает список доступных сотрудников (по известным tg_id) и после выбора перезаписывает `executor`/`executor_tg_id`, фиксируя `updated_by`.
- Команда `/reset <shop_id>` очищает активную смену магазина: статус возвращается в `waiting`, `executor` становится «—», а время последнего обновления помечает сброс (митигирует зависшие смены).
- Менеджер может вручную скорректировать строку в Google Sheets — бот просто прочитает факт смены и будет оповещать нового исполнителя.

### Преимущества

- Не нужен заранее согласованный график: система фиксирует первого фактического исполнителя.
- Работает даже без username, т.к. бот хранит Telegram ID ещё при `/start`.
- Менеджер видит историю смен прямо в таблице и может быстро вмешаться (кнопка или ручная правка).
- Минимальная бюрократия для пилота — можно стартовать без отдельного roster и перейти к «Варианту 1» позже.

### Ограничения

- Бот не знает, кто должен выйти сегодня, поэтому шлёт напоминания всем сотрудникам магазина, пока статус `waiting`.
- При одновременном нажатии побеждает первый, кто дошёл до записи в Google Sheets; второй сразу увидит предупреждение про занятую смену.
- Иногда менеджеру нужно вручную передать смену или сделать `/reset`, если кассир не закрыл чек-лист и статус завис в `in_progress`.
