# План задач — Живой ассистент (Conversational mode)

Версия: 21.11.2025

## 0. Общее
- Репозиторий: agronom-bot.
- Цель: внедрить режим живого ассистента, описанный в `docs/prd.md` (U4), `docs/srs.md` §4.6, `docs/API.md`, `openapi/openapi.yaml` и `docs/UX/flows.md`: чат = интерфейс, данные = cases/plans/events/reminders.
- Все PR → develop, DoD из PRD/SRS обязательны. После каждого шага обновлять документацию/локали при изменении поведения.
- Отмечать выполнение задач прямо в этом плане (или связанном тикете): ставить чекбокс/✅, дату, коротко указать пройденные тесты/линты. Если задача заблокирована — пометка `⚠ blocked: причина, дата`.

## 1. API ассистента — ✅ 23.11.2025 (pytest tests/test_assistant.py tests/test_api.py::test_openapi_schema, ALLOW_OLD_PYTHON=1)
- **Описание:** реализовать `/v1/assistant/chat` и `/v1/assistant/confirm_plan` (FastAPI).
- **Детали:** схемы по OpenAPI; авторизация как у остальных v1; логировать `proposal_id/user_id/object_id`.
- **DoD:** openapi-diff/Spectral проходят; интеграционные тесты на успешный ответ и ошибки валидации (нет object_id/культуры/дозы/времени).
- **Зависимости:** готовые PlanService/EventService/WeatherService.

## 2. Оркестратор (LLM + tool-calls) — ✅ 23.11.2025 (контекст loader, простые предложения; unit e2e пока не покрыты)
- **Описание:** сервис AssistantOrchestrator, собирающий контекст (active object, recent diagnosis, last plans/events, погода) и обрабатывающий tool-calls.
- **Детали:** использовать существующие сервисы `get_objects`, `get_recent_diagnosis`, `create_plan/update_plan`, `create_event/update_event_status`, `autoplan/weather`; запрет на создание «сырых» планов без минимальных полей.
- **DoD:** unit-тесты на подбор контекста, обработку tool-calls, возврат userErrors (OBJECT_NOT_FOUND, PHI_CONFLICT, WEATHER_UNAVAILABLE).
- **Зависимости:** задача 1.

## 3. Подтверждение «📌 Зафиксировать» — ✅ 23.11.2025 (stub PlanService + события/напоминания, pytest)
- **Описание:** конвертировать `proposal_id` в структуры (draft → proposed → accepted/scheduled) через PlanService/EventService.
- **Детали:** без времени — вызывать автоплан или открывать ручной мастер; при конфликте с активным планом — diff/подтверждение как в SRS 4.5.4.
- **DoD:** интеграционные тесты confirm_plan (success, missing fields, PHI conflict); идемпотентность по proposal_id.
- **Зависимости:** задачи 1–2.

## 4. Telegram-бот
- **Описание:** добавить вход «💬 Задать вопрос ассистенту» и обработку CTA «📌 Зафиксировать».
- **Детали:** хранить `session_id`, отправлять `/assistant/chat`, показывать предложения с кнопкой фиксации; по confirm — подтверждение + CTA «Показать дневник». Не ломать `/plan_treatment` и plan_sessions.
- **DoD:** jest/e2e сценарий «чат → предложение → фиксация → события созданы», UX соответствует `docs/UX/flows.md`.
- **Зависимости:** задачи 1–3.

## 5. Хранение и метрики
- **Описание:** опционально таблица `assistant_proposals` (proposal_id, user_id, object_id, payload, status, plan_id, event_ids, created_at); метрики Prometheus.
- **Детали:** идемпотентность по proposal_id; счётчики запросов/успехов/ошибок; audit trail для расследований.
- **DoD:** таблица/ORM модели готовы; метрики видны в /metrics; алерты на высокий error-rate.
- **Зависимости:** задача 1.

## 6. Локали и UX
- **Описание:** добавить строки для «💬 Задать вопрос ассистенту», «📌 Зафиксировать», ошибок ассистента (OBJECT_NOT_FOUND, PRODUCT_FORBIDDEN, PHI_CONFLICT, BUTTON_EXPIRED, SESSION_EXPIRED).
- **Детали:** сохранить тональность («Давай», «Советую»); не дублировать тексты мастера, переиспользовать userErrors.
- **DoD:** локали обновлены, ссылки в bot-коде; ручная проверка RU.
- **Зависимости:** задача 4.

## 7. Автоплан и воркер
- **Описание:** запуск автоплана по `autoplan_runs` ассистента.
- **Детали:** эндпоинт `/plans/{id}/autoplan` → запись в `autoplan_runs`, воркер `worker/autoplan.js` подхватывает pending (добавлен поллер). Привести option_id/stage_id в порядок.
- **DoD:** run создаётся, воркер подхватывает и ставит слот/напоминания; метрики/логи видны.
- **Статус:** в работе (есть запись в БД + поллер; очередь BullMQ/метрики не покрыты).

## 8. QA и релиз
- **Описание:** финальные проверки, `ruff check app tests`, `pytest`, `npm test --prefix bot`, openapi-diff/Spectral.
- **Детали:** добавить записи в `CHANGELOG.md`; обновить README при изменении поведения.
- **DoD:** все тесты/линтеры зелёные; PR содержит тест-план и ссылки на тикеты.

## 9. Backlog / vNext
- Улучшить память ассистента (vector store или recent context).
- Режим «повтори последнее» (быстрая фиксация предложений без полного диалога).
- Поддержка мультиязычных локалей (если появится en/kk).
