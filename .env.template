# ======================
# 🔑 OpenAI API Settings
# ======================
OPENAI_API_KEY=sk-...
# Модель для GPT‑Vision (опционально переопределите при необходимости)
OPENAI_VISION_MODEL=gpt-5
# Опциональная резервная модель (например, gpt-4o-mini), используется при timeout основной
OPENAI_VISION_MODEL_FALLBACK=gpt-4o-mini
# Таймаут запроса к OpenAI в секундах (по умолчанию 60)
OPENAI_TIMEOUT_SECONDS=60
# Температура выборки (оставьте пустым, чтобы использовать значение по умолчанию модели)
OPENAI_TEMPERATURE=0
# Модель для GPT‑Vision (опционально переопределите при необходимости)
OPENAI_VISION_MODEL=gpt-5
# Температура выборки (оставьте пустым, чтобы использовать значение по умолчанию модели)
OPENAI_TEMPERATURE=0

# Прокси для OpenAI/HTTP (если используется)
HTTP_PROXY=http://user:pass@host:port
HTTPS_PROXY=http://user:pass@host:port
# Локальные Docker-сервисы, которые нужно обходить мимо прокси
NO_PROXY=localhost,127.0.0.1,api,bot,db,redis,minio,prometheus,loki,grafana

# ==============================
# 🤖 Telegram Bot Configuration
# ==============================
# Токен Telegram-бота для разработки (BotFather)
BOT_TOKEN_DEV=your-dev-telegram-bot-token-here

# Настройки доступа бота к внутреннему API
API_BASE_URL=http://localhost:8010
API_KEY=test-api-key
API_VER=v1
# Порт HTTP-сервера Prometheus метрик бота
BOT_METRICS_PORT=3000

# Токен Telegram-бота для продакшена (BotFather)
# BOT_TOKEN_PROD=your-prod-telegram-bot-token-here

# ========================
# 🗄️  Database/PostgreSQL
# ========================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=your_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# SQLAlchemy строка подключения (используется Alembic)
# Перед запуском миграций укажите эту переменную
DATABASE_URL=sqlite:///./app.db
BOT_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agronom
# Redis connection string for rate limiting and queues
REDIS_URL=redis://localhost:6379
# Comma-separated list of trusted proxy IPs for X-Forwarded-For
TRUSTED_PROXIES=127.0.0.1,testclient

# ==========================
# ⚙️ FastAPI / Flask Configs
# ==========================
SECRET_KEY=replace-me
DEBUG=True
APP_ENV=development   # development / production / staging

# =======================
# 🔒 Security / HMAC / JWT
# =======================
HMAC_SECRET=test-hmac-secret
HMAC_SECRET_PARTNER=test-hmac-partner
# JWT_SECRET=your-jwt-secret (если используется JWT)
# ALLOWED_ORIGINS=http://localhost:3000 (если нужен CORS)

# ================
# ☁️ S3 / Minio
# ================
S3_BUCKET=agronom
S3_REGION=us-east-1
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_PUBLIC_URL=http://localhost:9000

# =========================
# 🛠️ Optional Dev Settings
# =========================
# LOG_LEVEL=info
# TELEGRAM_WEBHOOK_URL=https://your-domain.com/webhook
# TEST_USER_ID=111111111
# MOCK_MODE=True

# =========================
# 🧪 External Integrations
# =========================
TINKOFF_TERMINAL_KEY=your-terminal-key
TINKOFF_SECRET_KEY=your-secret-key
# AGROSTORE_API_KEY=your-agrostore-api-key-here
# PARTNER_WEBHOOK_SECRET=partner-secret
# PARTNER_LINK_BASE=https://agrostore.ru/agronom

# =======================
# 🏷️  Misc/Other Settings
# =======================
# TZ=Europe/Moscow
# LANG=ru_RU.UTF-8
# Number of free diagnoses per month
FREE_MONTHLY_LIMIT=5
# Number of free photos for Telegram bot per month
FREE_PHOTO_LIMIT=5
PAYWALL_ENABLED=true
RETRY_CRON=0 1 * * *
RETRY_CONCURRENCY=1
RETRY_LIMIT=3
PRIVACY_URL=
BETA_EXPERT_CHAT=false
FEEDBACK_URL=https://example.com/feedback

# =========================
# 📚 Catalog Import SSL
# =========================
# Verify TLS certificates when downloading protocol archives
CATALOG_SSL_VERIFY=true
# Path to a custom CA bundle file
CATALOG_CA_BUNDLE=
